import{useTranslation}from'react-i18next';import{ALL_NAMESPACES}from'../i18n/config';import{useLanguageContext}from'../context/LanguageContext';import{useCallback}from'react';import{availableLanguages,LanguageMetadata}from'../i18n/types';export{availableLanguages};export type{LanguageMetadata};const LOCALE_MAP:Record<string,string>={id:'id-ID',en:'en-US',jv:'id-ID',slg:'id-ID'};const DEFAULT_LOCALE='id-ID';const getLocale=(langCode:string):string=>LOCALE_MAP[langCode]??DEFAULT_LOCALE;const parseDate=(date:Date|string|number):Date=>{if(date instanceof Date)return date;if(typeof date==='string'&&/^\d{4}-\d{2}-\d{2}$/.test(date))return new Date(`${date}T00:00:00`);return new Date(date)};export function useLocale(){const{t,i18n}=useTranslation(ALL_NAMESPACES as unknown as string[]);const{language:currentLanguage,setLanguage,languageMeta:currentLanguageMeta,availableLanguages:langs}=useLanguageContext();const formatDate=useCallback((date:Date|string|number,options?:Intl.DateTimeFormatOptions):string=>{const d=parseDate(date);if(isNaN(d.getTime()))return'-';try{return new Intl.DateTimeFormat(getLocale(currentLanguage),options??{day:'numeric',month:'long',year:'numeric'}).format(d)}catch{return new Intl.DateTimeFormat(DEFAULT_LOCALE,{day:'numeric',month:'long',year:'numeric'}).format(d)}},[currentLanguage]);const formatDateTime=useCallback((date:Date|string|number,options?:Intl.DateTimeFormatOptions):string=>{const d=parseDate(date);if(isNaN(d.getTime()))return'-';try{return new Intl.DateTimeFormat(getLocale(currentLanguage),options??{day:'numeric',month:'long',year:'numeric',hour:'2-digit',minute:'2-digit'}).format(d)}catch{return String(d)}},[currentLanguage]);const formatTime=useCallback((date:Date|string|number,options?:Intl.DateTimeFormatOptions):string=>{const d=parseDate(date);if(isNaN(d.getTime()))return'-';try{return new Intl.DateTimeFormat(getLocale(currentLanguage),options??{hour:'2-digit',minute:'2-digit'}).format(d)}catch{return String(d)}},[currentLanguage]);const formatCurrency=useCallback((amount:number):string=>{if(!isFinite(amount)||isNaN(amount))return'Rp -';try{return new Intl.NumberFormat(getLocale(currentLanguage),{style:'currency',currency:'IDR',minimumFractionDigits:0,maximumFractionDigits:0}).format(amount)}catch{return`Rp ${amount.toLocaleString('id-ID')}`}},[currentLanguage]);const formatNumber=useCallback((num:number):string=>{if(!isFinite(num)||isNaN(num))return'-';try{return new Intl.NumberFormat(getLocale(currentLanguage)).format(num)}catch{return String(num)}},[currentLanguage]);const formatRelativeTime=useCallback((value:number,unit:Intl.RelativeTimeFormatUnit):string=>{try{return new Intl.RelativeTimeFormat(getLocale(currentLanguage),{numeric:'auto'}).format(value,unit)}catch{return`${Math.abs(value)} ${unit} lalu`}},[currentLanguage]);const formatTimeAgo=useCallback((date:Date|string|number):string=>{const d=parseDate(date);if(isNaN(d.getTime()))return'-';const now=new Date();const diffMs=d.getTime()-now.getTime();const diffS=Math.round(diffMs/1000);const diffM=Math.round(diffS/60);const diffH=Math.round(diffM/60);const diffD=Math.round(diffH/24);const diffW=Math.round(diffD/7);const diffMo=Math.round(diffD/30);const diffY=Math.round(diffD/365);let value:number,unit:Intl.RelativeTimeFormatUnit;if(Math.abs(diffS)<60){value=diffS;unit='second'}else if(Math.abs(diffM)<60){value=diffM;unit='minute'}else if(Math.abs(diffH)<24){value=diffH;unit='hour'}else if(Math.abs(diffD)<7){value=diffD;unit='day'}else if(Math.abs(diffW)<5){value=diffW;unit='week'}else if(Math.abs(diffMo)<12){value=diffMo;unit='month'}else{value=diffY;unit='year'};try{return new Intl.RelativeTimeFormat(getLocale(currentLanguage),{numeric:'auto'}).format(value,unit)}catch{return`${Math.abs(value)} ${unit} lalu`}},[currentLanguage]);return{currentLanguage,currentLanguageMeta,setLanguage,availableLanguages:langs,t,i18n,formatDate,formatDateTime,formatTime,formatCurrency,formatNumber,formatRelativeTime,formatTimeAgo}}